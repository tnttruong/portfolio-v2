export interface ExperienceProps {
    title: string;
    date: string;
    summary: string;
}

export interface ProjectProps {
    img_url: string;
    title: string;
    stack: string[];
    link: string;
}

export interface ProjectItemProps {
    title: string;
    img_url: string;
    stack: string[];
    link: string;
}